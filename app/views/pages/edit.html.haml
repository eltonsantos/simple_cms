- content_for(:title) { "Edit Page: #{@page.name}" }

= link_to("<< Back to List", pages_path, :class => 'back-link')
.pages.edit
  %h2 Update Page
  = form_for(@page) do |f|
    = render(:partial => 'form', :locals => {:f => f})
    .form-buttons
      = f.submit("Update Page")